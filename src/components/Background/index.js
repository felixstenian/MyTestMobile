import LinearGradiente from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default styled(LinearGradiente).attrs({
  colors: ['rgb(50, 127, 290)', '#00BFFF'],
})`
  flex: 1;
`;
